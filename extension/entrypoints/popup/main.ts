import './style.css';

// TODO(§1.4): render badge state + confidence in honest language, per-source evidence
// list, NO_SIGNAL notice, and local history from chrome.storage.local (last 50 videos,
// searchable). See docs/SPEC.md §3.3 for the observed-state taxonomy to render.
export {};
